{
  "name": "agentflow",
  "version": "1.0.0-beta.7",
  "description": "Deterministic, composable iteration inside a single Claude Code session, shipped as a plugin.",
  "license": "Apache-2.0",
  "type": "module",
  "author": {
    "name": "Alessandro Saiani",
    "email": "11213486+AleSaiani@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AleSaiani/agentflow.git"
  },
  "homepage": "https://github.com/AleSaiani/agentflow",
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "iteration",
    "enumerate",
    "agent",
    "workflow"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "tsc --build --clean",
    "test": "tsc && node --test \"test/**/*.test.ts\"",
    "validate": "claude plugin validate ."
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^6.0.0"
  }
}
